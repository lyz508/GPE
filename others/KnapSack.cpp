#include <iostream>
#include <cstdio>
#include <vector>
using namespace std;

const int numObj = 3, numWeight=20;
int dp[numObj+1][numWeight+1],
    w[numObj], v[numObj];

int main(){
    printf("value and weight: ");
    for (int i=0; i<3; i++)
        cin >> v[i] >> w[i];

    /*Knap sack*/
    

    return 0;
}